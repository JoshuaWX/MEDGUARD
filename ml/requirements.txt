# MedGuard ML pipeline (Phase 1: data + training foundation).
# Pure offline Python — does not touch the Expo app or Supabase edge runtime.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
requests>=2.31           # NASA POWER + Supabase REST
pdfplumber>=0.11         # parse NCDC Lassa situation-report PDFs (ingest/)
python-dotenv>=1.0       # load ml/.env (git-ignored)
joblib>=1.3              # persist the trained model
rasterio>=1.3            # sample Malaria Atlas Project incidence raster (ingest/map_incidence.py)
