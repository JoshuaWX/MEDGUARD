---
interface Props { eyebrow: string; title: string; description: string; }
const { eyebrow, title, description } = Astro.props;
---
<header class="page-hero"><div class="shell"><span class="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div></header>
